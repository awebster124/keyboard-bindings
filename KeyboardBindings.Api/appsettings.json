{
  "ConnectionStrings": {
    // NOTE: In a real deployment this would be supplied by an environment variable (or a secret
    // store), not committed to source. It lives here purely to keep local setup friction-free.
    "Default": "Data Source=keyboardbindings.db"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
